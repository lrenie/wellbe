<% content_for :meta_title, " Session de sport on #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, @session.difficulty %>
<% content_for :meta_image, cl_image_path(multi.jpg) %>

<% if (@session.mode == "multi") %>
<%= render "show_multi" %>
<% end %>
